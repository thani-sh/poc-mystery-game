{
  "name": "asset-copier",
  "version": "1.0.0",
  "description": "CLI tool to copy and prepare game assets with background removal",
  "type": "module",
  "bin": {
    "asset-copier": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "dev": "bun run ./src/index.ts"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
